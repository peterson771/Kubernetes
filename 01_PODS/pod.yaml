apiVersion: v1
kind: Pod
metadata:
  name: primeiro-pod
  labels:
    name: primeiro-pod
spec:
  containers:
  - name: primeiro-pod
    image: nginx
    resources:
      limits: # limite de recursos utilizado no pod
        memory: "128Mi"
        cpu: "500m"

